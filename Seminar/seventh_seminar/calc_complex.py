#Операция с комплексными числами

def calc_complex_main(a, b, move):
    if move == '*':
        return a * b
    if move == '/':
        return a / b
    if move == '+':
        return a + b
    if move == '-':
        return a - b

calc_complex_main(a, b, c)


